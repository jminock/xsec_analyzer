tutorial_1D_proton
phaseIITriggerTree
6
0 1 "trueCC && trueFSLPdg == 13 && trueTrackLengthInMRD > 0. && trueFV && mc_no_fs_mesons && trueMuonMomentum >= 600 && trueMuonMomentum < 740"
0 1 "trueCC && trueFSLPdg == 13 && trueTrackLengthInMRD > 0. && trueFV && mc_no_fs_mesons && trueMuonMomentum >= 740 && trueMuonMomentum < 860"
0 1 "trueCC && trueFSLPdg == 13 && trueTrackLengthInMRD > 0. && trueFV && mc_no_fs_mesons && trueMuonMomentum >= 860 && trueMuonMomentum < 1000"
0 1 "trueCC && trueFSLPdg == 13 && trueTrackLengthInMRD > 0. && trueFV && mc_no_fs_mesons && trueMuonMomentum >= 1000 && trueMuonMomentum < 1100"
0 1 "trueCC && trueFSLPdg == 13 && trueTrackLengthInMRD > 0. && trueFV && mc_no_fs_mesons && trueMuonMomentum >= 1100 && trueMuonMomentum < 1200"
1 -1 "trueCC != 1 || trueFSLPdg != 13 || trueTrackLengthInMRD <= 0. || trueFV != 1 || mc_no_fs_mesons != 1 || trueMuonMomentum < 600 || trueMuonMomentum >= 1200"
5
0 1 "simpleRecoFlag==1 && reco_contained_in_MRD && simpleRecoFV && RCSRPred > 0.2 && simpleRecoMomentumCor >= 600 && simpleRecoMomentumCor < 740"
0 1 "simpleRecoFlag==1 && reco_contained_in_MRD && simpleRecoFV && RCSRPred > 0.2 && simpleRecoMomentumCor >= 740 && simpleRecoMomentumCor < 860"
0 1 "simpleRecoFlag==1 && reco_contained_in_MRD && simpleRecoFV && RCSRPred > 0.2 && simpleRecoMomentumCor >= 860 && simpleRecoMomentumCor < 1000"
0 1 "simpleRecoFlag==1 && reco_contained_in_MRD && simpleRecoFV && RCSRPred > 0.2 && simpleRecoMomentumCor >= 1000 && simpleRecoMomentumCor < 1100"
0 1 "simpleRecoFlag==1 && reco_contained_in_MRD && simpleRecoFV && RCSRPred > 0.2 && simpleRecoMomentumCor >= 1100 && simpleRecoMomentumCor < 1200"
